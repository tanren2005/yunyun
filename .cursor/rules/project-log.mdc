---
description: 每次改动必须追加写入 PROJECT_LOG.md
alwaysApply: true
---

# 开发日志必须更新

任何实现、修复、换方案、配置或文档改动完成后，**同一回合**追加写入仓库根目录 `PROJECT_LOG.md`。

## 写入格式

在「变更履历」最上方追加一条（最新在上）：

```markdown
### YYYY-MM-DD — 简短标题

- **做了什么**：改了哪些文件、加了什么功能
- **原方案 → 新方案**：若有替换，写清本来用什么、为何不用、改成什么
- **原因**：用户要求 / 报错 / 性能 / 网络等
```

## 不要

- 用空文件覆盖整份日志
- 只改代码不记日志
- 把密钥、授权码、验证码原文写进日志
